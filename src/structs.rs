pub struct BoxStruct
{
    pub x_pos: u32,
    pub y_pos: u32,
    pub tracking_number: i64,
    pub destination: i64
}

pub struct ConnectionStruct
{
    pub source_node: u32,
    pub destination_node: u32,
    pub weight: u32
}